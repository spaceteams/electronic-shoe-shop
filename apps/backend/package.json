{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "biome lint --write ERP __tests__",
    "fmt": "biome format --write ERP __tests__",
    "test": "jest",
    "test:ci": "jest --ci --coverage"
  },
  "devDependencies": {
    "@aws-sdk/util-dynamodb": "3.996.9",
    "@types/aws-lambda": "8.10.163",
    "@types/jest": "^29.5.0",
    "@types/node": "^24.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0"
  }
}
